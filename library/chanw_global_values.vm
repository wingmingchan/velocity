#*
This format turns the strings defined in chanw_global_values_code into code
and executes it.
*#
## reset this name if it is different; this name is used throughout the entire core
#set( $master_site_name  = "_common_assets" )
#set( $library_namespace = "chanw" )

#set( $global_values_code = $_.locateBlock( 
    "formats/library/velocity/${library_namespace}_global_values_code", 
    $master_site_name ).getXMLAsXMLElement().getChild( "global-values" ).Value )
#set( $global_values_code = $global_values_code.replaceAll( "&amp;", "&" ).replaceAll( "&lt;", "<" ).replaceAll( "&gt;", ">" ) )

#evaluate( $global_values_code )