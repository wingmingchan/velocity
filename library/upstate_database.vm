#*
This format populates list variables with values drawn from the database.
To use this format:

#import( 'site://_common_assets/formats/library/velocity/upstate_database' )

#foreach( $site in $upstateSiteName )
    $site
#end
*#
#import( 'site://_common_assets/formats/library/velocity/chanw_library_import' )

#set( $list_sql_map = { "upstateSiteName"  : "SELECT name FROM cxml_site", 
                        "upstateUserName"  : "SELECT username FROM cxml_user",
                        "upstateGroupName" : "SELECT name FROM cxml_group"
                      } )

#foreach( $key in $list_sql_map.keySet() )
    #set( $sql = $list_sql_map[ $key ] )
    #chanwGetDatabaseList( $key $sql )
#end

#chanwCleanUp